import{Link}from"react-router-dom";import{useDocumentTitle}from"../hooks/useDocumentTitle";
// Also rendered in place by pages whose route matched but whose record doesn't
// exist (an unknown product or collection), with copy and a CTA to suit.
export default function NotFound({title="Page not found",heading=<>That page isn't<br/><span>in the nest.</span></>,message="The page you requested does not exist.",primary={to:"/",label:"Back home"},secondary={to:"/products",label:"Browse products"}}){useDocumentTitle(title);return <main className="not-found container"><span className="eyebrow">404</span><h1>{heading}</h1><p>{message}</p><div><Link to={primary.to} className="button primary">{primary.label}</Link>{secondary&&<Link to={secondary.to} className="button ghost">{secondary.label}</Link>}</div></main>}
