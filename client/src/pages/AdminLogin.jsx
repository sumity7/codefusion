import { useState } from "react";
import { Link,useNavigate,useSearchParams } from "react-router-dom";
import { ShieldCheck } from "lucide-react";
import { api } from "../services/api";
export default function AdminLogin(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);const nav=useNavigate();const[params]=useSearchParams();async function submit(e){e.preventDefault();setError("");setLoading(true);try{const x=await api.auth.adminLogin({email,password});localStorage.setItem("codefusion_token",x.token);const next=params.get("next")||"/admin";nav(next)}catch(err){setError(err.message)}finally{setLoading(false)}}return <main className="auth-page"><div className="auth-card admin-login-card"><div className="admin-login-icon"><ShieldCheck size={22}/></div><span className="eyebrow">CODEFUSION ADMIN</span><h1>Control center.</h1><p>Sign in with an administrator account to manage products, content and orders.</p><form onSubmit={submit}><label>Admin email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@example.com" autoComplete="username"/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••" autoComplete="current-password"/></label>{error&&<div className="form-error">{error}</div>}<button className="button primary" disabled={loading}>{loading?"Signing in…":"Enter admin"}</button></form><Link to="/forgot-password" className="auth-link">Forgot your password?</Link><Link to="/" className="auth-link">← Back to storefront</Link></div></main>}
