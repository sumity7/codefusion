import{useState}from"react";import{Link,useNavigate}from"react-router-dom";import{api}from"../services/api";
export default function Register(){const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const nav=useNavigate();async function submit(e){e.preventDefault();try{const x=await api.auth.register({name,email,password});localStorage.setItem("codefusion_token",x.token);nav("/account")}catch(err){setError(err.message)}}return <main className="auth-page"><div className="auth-card"><span className="eyebrow">JOIN CODEFUSION</span><h1>Create your account.</h1><p>Keep your wishlist, purchases and downloads in one place.</p><form onSubmit={submit}><label>Name<input required value={name} onChange={e=>setName(e.target.value)}/></label><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" minLength="8" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="form-error">{error}</div>}<button className="button primary">Create account</button></form><Link to="/login" className="auth-link">Already have an account? Sign in →</Link></div></main>}
