import {useEffect,useState} from "react";import{Heart}from"lucide-react";import{Link}from"react-router-dom";import{api}from"../services/api";import{fallbackProducts}from"../data/fallbackProducts";import ProductCard from "../components/ProductCard";
export default function Wishlist(){const[items,setItems]=useState([]);useEffect(()=>{api.wishlist.list().then(async x=>{const all=(await api.products.list()).products;setItems(all.filter(p=>x.products.includes(p.slug))) }).catch(()=>{})},[]);return <main className="simple-page container"><span className="eyebrow">YOUR COLLECTION</span><h1>Wishlist</h1><p>Save the pieces you want to revisit.</p>{items.length?<div className="product-grid">{items.map(p=><ProductCard key={p.slug} product={p}/>)}</div>:<div className="empty-state"><Heart size={23}/><h3>Your wishlist is empty</h3><p>Sign in and save a product to see it here.</p><Link to="/products" className="button primary">Browse products</Link></div>}</main>}
