import { useEffect,useState } from "react";
import { api } from "../services/api";
export function useAuth(){const[user,setUser]=useState(null);const[loading,setLoading]=useState(true);useEffect(()=>{const token=localStorage.getItem("codefusion_token");if(!token){setLoading(false);return}api.auth.me().then(x=>setUser(x.user)).catch(()=>localStorage.removeItem("codefusion_token")).finally(()=>setLoading(false))},[]);const login=async(body)=>{const x=await api.auth.login(body);localStorage.setItem("codefusion_token",x.token);setUser(x.user);return x};const register=async(body)=>{const x=await api.auth.register(body);localStorage.setItem("codefusion_token",x.token);setUser(x.user);return x};const logout=()=>{localStorage.removeItem("codefusion_token");setUser(null)};return{user,loading,login,register,logout}}
